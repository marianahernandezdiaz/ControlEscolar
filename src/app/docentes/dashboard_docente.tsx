import { Text, View } from "react-native";

export default function DashboardDocente() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
      }}
    >
      <Text>Dashboard Docente 👨‍🏫</Text>
    </View>
  );
}